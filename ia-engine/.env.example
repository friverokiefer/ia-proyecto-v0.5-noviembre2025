# =====================================
# IA ENGINE / OPENAI
# =====================================

OPENAI_API_KEY=
# API Key de OpenAI usada por el microservicio IA Engine

OPENAI_TEXT_JSON=gpt-4o-mini # Modelo base para respuestas en JSON (usado por text_engine.py)
OPENAI_TEXT_QUALITY=gpt-4o # Modelo de más calidad (por si quieres diferenciar casos “premium”)

# =====================================
# PARÁMETROS DE GENERACIÓN
# =====================================

OPENAI_TEXT_TEMP=0.6 # Temperatura (0.0 – 1.0). Más alto = más creativo, menos determinista
OPENAI_TEXT_TOP_P=0.9 # top_p para nucleus sampling
OPENAI_TEXT_MAX_TOKENS=900 # Máximo de tokens de salida

# =====================================
# LOGGING
# =====================================

LOG_LEVEL=INFO # Nivel de logging del microservicio (DEBUG|INFO|WARNING|ERROR)
